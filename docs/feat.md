- [x] M001: Melhorar a geração da arquitetura, na arquitetura não precisa ir o codigo usado no projecto, na arquitetura deve ir apenas a forma com que o projecto está estuturado de forma geral, não especificando cada elemento, porque no final fica um ficheiro muito longo com cada implementação, e essa não é a ideia... A ideia é que esse arquivo seja a base da construção do projecto, e não um log de tudo que está a ser immplementado... Por exeemplo, em alguns projecto foi inserido na arquitetura a fase do projecto, as funções usadas no dashboard, explicação de alguma coisa usada porque é mvp, migrações de uma tarefas específica, scripts sql. Isso é desnecessário, na arquitetura são as regras, estruturas de pastas, a forma de codificação, convenções, padrões, etc, tudo isso de forma geral e não especificando, porque os detalhes especificos de cada tarefa ou plano, já terão os seus próprios arquivos. Devemos fazer com que o agente que gera\actualiza essa arquitetura (architecture.md) tenha em conta isso, e faça actualizações até em projectos já existentes, eleiminado o desnecessário.

- [ ] M002: Implementar uma funcionalidade para lista de pastas permitidas predefinidas, que estão fora do directorio do projecto

- [ ] M003: Melhorar a integração de executar o agente em várias sessões, ou seja, nesse momento eu tenho um mono repo (mas não o convecional) que tem 3 partes (mobile, web, desktop) e em cada um deles eu posso inicializar o loop-development, e ter o loop-development geral que fica na raiz do projecto, por que nesse momento o agente só axecuta funcionalidade por funcionalidade, e se eu estiver a desenvolver em 2 projectos ao mesmo tempo, assim os arquivos de estados não são sobre-postos, como desse jeito já funciona que é iniciar em cada projecto, agora eu gostaria de ter alguma forma de ligar os projecto, referrenciando o principal e no principal referenciando os filhos ou os subprojectos iniciados ou algo parecido, isso deve ser feito meio que automático, se ao executar o loop-development ele detetar que em algum outro projecto foi iniciado o loop-development, então ele faz a ligação automática, reconhecendo o principal e os ficlhor, o principal é sempre o que está na raiz principal e os ficlhor é sempre que estão na raiz dos subdirectorios.

Ex.:
- projecto-x (pasta raiz)
-- .loop-development (main)
--- pasta-x
---- .loop-development (child)
--- pasta-z
---- .loop-development (child)

E assim eu consigo executar alguma tarefa num subprojecto, e se ele reconhece o main pelo menos ele em acesso a pasta razi também, porque se o loop-development estiver acessar uma pasta fora do seu directorio ele pede permissão, mas com essa implementação e ele reconhecer que ele faz parte do projecto todo, então não precisa mas de permissão, ele pode acessar normalmente.

- [ ] M004: Reforçar aos agentes que nunca devem documentar(inserir em qualquer arquivo) dados sensiveis de variaveis de ambiente, especificar apenas o nome da variavel de ambiente, onde será encontrada ou configurada, sem mostrar o valor dela. Devemos fazer com que isso seja verificado em projectos já existentes também, para evitar que valores de variaveis sensiveis estejam expostos, e se já estiver commitado, então deve ser feito uma edição no commit

- [ ]M005: Vamos melhorar os agentes que fazem a ferificação, eliminado a verificação do build em cada tarefa, mantendo apenas no final do plano todo, ou seja, a verificação fazendo o build do projecto deve ser feita apenas no final do plano, e caso exitam erro fazer a correção... Isso porque fazer o build a cada tarefa, acaba fazendo com que seja um processo muito demorado, porque o build consome muita capacidade computacional, então se estiver apenas no final de cada plano, melhora a velocidade com que cada plano é executado. O mesmo devemos fazer para os testes, mas para os testes, mas se existir uma forma de executar apenas os teste que aquela tarefa afecta, entõa pode ser executada, mas se for para executar os testes de forma geral, então é melhor inserir também no final, assim que todas as tarefas são concluidas e faz-se a verifcação do plano

- [ ] M006: Em alguns agentes, criam muitos arquivos redundantes, ao fazer o summaries, em alguns casos tem muita coisa que já esplicita no plano (spec.md) como por exemplo as decisões e as fase, eu não preciso voltar a inserir todos esses dados novamente, visto que já tenho um arquivo com esses dados... Nesse sentido, vamos melhorar os agentes, para evitar repetir dados que já tem em algum arquivo, se precisar, apenas mensione o arquivo onde encontrar aquela informação, em vez de voltar a repetir toda ela... Assim também tornamos os arquivos mais compactos e menos redundantes... Porque na hora de inspecionar ve-se muita informação repetida e desnecessária

- [ ] M007: Devemos criar também um arquivo em cada plano, que será actualizado a cada tarefa terminada, explicando como testar aquela funcionalidade, correção ou melhoria, especificando onde deve entrar, qual é página, o que deve fazer para ver se está funcionar conforme o esperado, quais configurações deve fazer(a nivel do projecto ou arquitetural e a nivel da aplicação), para assim, no final de cada tarefa ou plano, eu consiga seguir as instruções para fazer o teste manual, sem precisar adivinhar. Porque no final de cada plano fica complicado saber o que foi feito porque é muita coisa para acompanhar.