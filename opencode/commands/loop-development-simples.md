---
description: Inicia o Loop Development em modo simple — planeia, implementa tarefa a tarefa e concentra toda a verificação no fim do plano (poupa contexto/tokens)
agent: loop-development
---

Recebi este pedido de projeto/funcionalidade para executar em modo `simple`: $ARGUMENTS

O modo deste plano é `simple` (fluxo enxuto: sem Research autónomo, Architecture Reviewer, Compacter, auditorias por tarefa nem Final Reviewer; testes e verificação final concentrados no fim). Segue rigorosamente o teu protocolo definido no system prompt, começando pela fase de Intake — que deve gravar `mode: "simple"` no estado do plano. Não saltes nenhuma fase aplicável ao modo e não implementes nada sem aprovação explícita do plano e da lista de tarefas.
